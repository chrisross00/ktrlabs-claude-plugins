---
description: First-run setup — install ffmpeg / whisper-cli / Whisper model, then probe macOS permissions so the permission dialog fires before your first real recording.
---

!`cd "${CLAUDE_PLUGIN_ROOT}" && python3 -m bin.setup_cli`
